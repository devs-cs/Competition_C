#include <iostream>
#include <string>
#include <cstring>
using namespace std;


Struct pair{
    int num;
    int length;
}


int main()
{

    int n, m;
    cin >> n; 
    cin >> m;

    vector<pair> 

    char query[m] = {};
    for(int i = 0; i < m; i++)
    {
        Struct pair 
    }





    for(int i = 0; i < q; i++)
    {
        string s;
        cin >> s;
        int a,b;
        cin  >> a;
        cin >> b;
        if(s == "XOR")
            cout << rangeComp(a-1,b-1)  <<endl;
        else if (s == "NEW")
            addToTree(a-1, b);
    }
    return 0;
}